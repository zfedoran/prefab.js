define([
    ],
    function(
    ) {
        'use strict';

        var ButtonState = function() {
        };

        ButtonState.prototype = {
            constructor: ButtonState,
        };

        ButtonState.BUTTON_DOWN = 'down';
        ButtonState.BUTTON_UP   = 'up';

        return ButtonState;
    }
);
